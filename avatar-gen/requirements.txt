# 아바타 파이프라인 (text→LLM→TTS→lip-sync)
openai>=1.30       # LLM (OpenAI 호환 provider)
piper-tts>=1.3     # TTS
numpy>=1.24
Pillow>=10.0       # placeholder 얼굴 생성
PyYAML>=6.0
# ffmpeg: 시스템 설치 필요(static 백엔드). brew install ffmpeg / apt install ffmpeg

# 립싱크 모델(선택, 별도 설치):
#   Wav2Lip   : git clone + wav2lip_gan.pth, torch 필요. NVIDIA=cuda 그대로, Mac=MPS 패치
#   SadTalker : 의존성 무거움(face-alignment, gfpgan), GPU 권장
#   MuseTalk/LatentSync : 최신 계열, GPU 권장
