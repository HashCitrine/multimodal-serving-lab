[project]
name = "lab-ui"
version = "0.1.0"
description = "멀티모달 서빙 랩 대시보드 (FastAPI). 각 실험을 uv run 으로 실행."
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "python-multipart>=0.0.9",
    "uvicorn>=0.29",
    "PyYAML>=6.0",
    "httpx>=0.27",
]

[tool.uv]
package = false
